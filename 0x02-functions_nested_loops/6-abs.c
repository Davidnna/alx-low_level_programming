#include "main.h"

/**
 * _abs - computes abs value
 * @n: the number given absolute value
 * Return: n if positive and -n otherwise
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
